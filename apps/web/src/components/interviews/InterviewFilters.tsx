export function InterviewFilters({
  value,
  onChange,
}: {
  value: Record<string, unknown>;
  onChange: (v: Record<string, unknown>) => void;
}) {
  return (
    <div className="rounded border p-3 text-sm text-slate-500">
      InterviewFilters — TODO
    </div>
  );
}
