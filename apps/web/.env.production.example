# REQUIRED at build time (not runtime) — must be set as an environment variable in Render.com before building
# This is the full URL of your API service on Render.com
VITE_API_URL=https://your-api-service.onrender.com
